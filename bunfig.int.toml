[test]
root = "tests/integration"
timeout = 120000
coverageReporter = ["text", "lcov"]
coverageDir = "coverage/int"
coverageSkipTestFiles = true
# Integration coverage measures src/adapters only; cli/lib are covered by the unit suite.
coveragePathIgnorePatterns = ["src/cli/**", "src/lib/**"]
